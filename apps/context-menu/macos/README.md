# macOS Finder Sync Extension

TODO(Phase 3, Week 9–10).

- Separate Xcode project.
- Signed with same Team ID as main Electron app.
- XPC service communicates with main app process.
- Menu item "Add to AI memory" + badge icon for indexed folders.

Spike task in Week 1 to verify code-sign + XPC path before committing to full build.
